"""
model.teleporteur
Simule le téléporteur (action de déplacement).
"""

from typing import Any


class Teleporteur:
    """
    Simulation d'un téléporteur. Aucune logique métier ici, seulement abstraction.
    """

    def transporter(self, immatriculation: str, id_place: int) -> None:
        """
        Simule l'action de transporter une voiture vers la place.
        """
        # Pas de print ici : la View gère l'affichage. Cette méthode reste neutre.
        return
